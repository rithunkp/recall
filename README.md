# Recall

Recall is a hackathon prototype for a multi-agent perception system that decides which
doorstep camera events are worth remembering.

The build follows `PROJECT_STATE.md` as the source of truth. Run `./run.sh` from the repo
root as the one-command entry point; the script will grow as each hour lands.
